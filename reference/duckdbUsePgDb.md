# Ensure duckdb is using Postgres as default catalog

Ensure duckdb is using Postgres as default catalog

## Usage

``` r
duckdbUsePgDb(con)
```

## Arguments

- con:

  DuckDb connection

## Value

DuckDb connection that uses Postgres as default catalog
