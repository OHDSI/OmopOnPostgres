# Check if duckdb is using Postgres as default catalog

Check if duckdb is using Postgres as default catalog

## Usage

``` r
duckdbIsUsingPgDb(con)
```

## Arguments

- con:

  DuckDb connection

## Value

TRUE if using Postgres as default catalog, otherwise FALSE
