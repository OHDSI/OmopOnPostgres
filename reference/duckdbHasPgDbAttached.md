# Check if a PostgreSQL database is attached to DuckDB

Check if a PostgreSQL database is attached to DuckDB

## Usage

``` r
duckdbHasPgDbAttached(con)
```

## Arguments

- con:

  DuckDb connection

## Value

TRUE if postgres database attached, otherwise FALSE
